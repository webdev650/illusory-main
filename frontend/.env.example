# Frontend API URL (points to the backend service)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# MongoDB Connection String (shared with backend)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?appName=<appName>

# Cloudinary Storage Credentials (shared with backend)
CLOUDINARY_CLOUD_NAME=dtmqv7oqq
CLOUDINARY_API_KEY=232257233914578
CLOUDINARY_API_SECRET=vK6dEFuZM05Z4jePX-4mSHIM0GE

# Nodemailer Email Settings (for notification and confirmation emails)
# Set these to enable custom SMTP (e.g. Gmail App Passwords, Resend, Sendgrid, etc.)
# If EMAIL_HOST is left blank, it defaults to Gmail service
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
EMAIL_TO=operations@illusorydesignstudios.com

# Admin Dashboard Access
ADMIN_PASSWORD=admin123
